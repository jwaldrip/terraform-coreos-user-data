-
    name: ${service}.service
    ${replace(replace(enabled, "true", "command: start"), enabled, "mask: true")}
    ${replace(drop-ins, "/^[\s\S]+$/", "drop-ins:\n      ${join("\n      ", split("\n", drop-ins))}")}
